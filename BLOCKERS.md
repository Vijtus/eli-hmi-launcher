| id | ticket | question | who answers | what is stubbed meanwhile | severity |
|---|---|---|---|---|---|
| BLK-001 | CSI-853, CSI-854 | The EPICS read/write gateway was found at `https://github.com/eli-eric/eli-hmi.git`, branch `dev`, under `backend/python-websocket-server`, but it has no launcher lifecycle endpoints. Is that repository also intended to own HMI registration and discovery? If yes, supply its base URL, authentication scheme, endpoint paths, payload schemas, heartbeat interval, idempotency/deduplication key, retry rules, and unreachable behavior. If no, where does that service live? | HMI API owner and control-system maintainers | An executable loopback-only lifecycle sidecar now proves the launcher adapter, leases, heartbeats, reservations, retries, and shutdown behavior against an explicitly local version-1 contract. Unconfigured launchers still use the no-op adapter and report `disabled`. The sidecar is not asserted to be the missing site contract. | Critical |
| BLK-002 | CSI-849 | Who will arrange the three alarm panels in the site Phoebus build and save the resulting `.memento`, and where will that file be deployed? | Control-system maintainers | A memento saved by the locked local Phoebus runtime is tracked and its three-panel restore was observed. It uses the local `CSI744_LOCAL` alarm root and is evidence for the mechanism only, not a site artifact. | High |
| BLK-011 | — | What item keys do the `labview-epics`, `css`, and `web` zone groups use? All three are empty in `eli-eric/eli-hmi-config`, so the launcher's adapter assumes keys mirroring the existing target types (`gui-name`/`gui-type`/`exe`, `name`/`resource`/`app`/`layout`, `name`/`url`). | Control-system maintainers | `labview-dev` is implemented against real data. The other three parse and validate against the assumed schema and are documented in README.md; an unknown group is skipped with a warning rather than failing. | Medium |
| BLK-012 | — | What is the Phoebus launcher script filename under `css-install` (`C:\CSS Phoebus\product-5.0.2`)? The launcher derives `local.phoebus.executable` as `<installRoot>\phoebus.bat` on Windows. | Phoebus/site maintainers | An explicit `local.phoebus.executable` in the host file overrides the derivation, so a wrong guess is correctable without a launcher change. | Medium |
| BLK-013 | CSI-853 | What are the scheme, port, and base path of the `hmi-server` value (`testz-deploy20:8082`), and is HTTPS available? A non-loopback `local.hmiApi.baseUrl` must be HTTPS and set `authTokenEnv`. | HMI API owner | `hmi-server` is preserved as `local.hosts."hmi-server"` and is NOT wired to `hmiApi.baseUrl`, so lifecycle networking stays disabled rather than failing validation. Related to BLK-001. | High |
| BLK-003 | CSI-848, CSI-849 | What internal application names does the site Phoebus build report from `-list` for the required alarm applications? | Phoebus/site maintainers | The locked ORNL build reported `display_runtime`, `alarm_tree`, `alarm_table`, and `alarm_area`; these names drive only the local acceptance assets. Catalog site values remain placeholders until the site build is queried. | High |
| BLK-004 | CSI-854 | What denotes LabVIEW HMI “write mode”: an argv flag, a PV, an API field, executable identity, or another signal? | LabVIEW HMI owners and control-system maintainers | The local contract labels both POSIX fixture launches as write mode so enforcement can be executed. That explicit test input does not define how a real LabVIEW HMI signals mode. | Critical |
| BLK-005 | CSI-854 | Is the required policy “only one LabVIEW HMI instance,” “only one write-mode instance,” or both by GUI type? | Ticket owner and control-system maintainers | Both policies are expressible. The local contract deliberately enables both and observes a lifecycle reservation conflict; deployed values still require the owner's answer. | Critical |
| BLK-006 | CSI-845 | Is the authoritative HMI catalog shared, per-machine, or layered, and which source owns conflict resolution? | Launcher maintainers and operations | ANSWERED for the git path: `eli-eric/eli-hmi-config` is authoritative, layered as zone (base) then host (override), injected as the last catalog source so it wins over inline and filesystem sources. Ordered local/filesystem sources and cache fallback remain for machines that have not set `ELI_LAUNCHER_CONFIG_REPO_URL`. | Resolved for git-backed machines |
| BLK-007 | CSI-744–CSI-854 | What are the real L4 GUI names, IOC names/types/hosts, EXE names, zone symbol, workspace root, CSS GUI root, web URLs, network-share path, Technology/Section/RMC/Note values, and logo asset? | L4 control-system maintainers | Example configuration contains named placeholders only; mock configuration remains non-site data. | High |
| BLK-008 | CSI-843, CSI-847 | Will Windows deployment use `.exe` wrappers or `cmd.exe /c` wrappers, and what `allowBareCommands` posture is approved? | Windows deployment and security maintainers | The launcher uses direct executable spawning with `shell: false`; no wrapper strategy is assumed. | High |
| BLK-009 | CSI-844 | Is TCP port 4918 reserved for Phoebus at the site, or was it only an example in the ticket? | Network/site Phoebus maintainers | The port remains configurable. Local acceptance used isolated port 14918; the deployment example still marks 4918 for confirmation. | High |
| BLK-010 | CSI-854 | If `onAlreadyRunning: focus` is required, what platform-supported identifier or API maps a registry record to the existing HMI window? | Windows/LabVIEW deployment maintainers | The policy is expressible, but the launcher refuses with a named error rather than claiming focus without a native window identity. | Medium |
